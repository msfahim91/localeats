name: Build APK
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.3'
          cache: false
      - name: Get dependencies
        run: |
          cd localeats
          flutter pub get
      - name: Build APK
        run: |
          cd localeats
          flutter build apk --release --no-tree-shake-icons -v 2>&1 | tail -50
      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: release-apk
          path: localeats/build/app/outputs/flutter-apk/app-release.apk
